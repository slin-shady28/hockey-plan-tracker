import { View, Text } from 'react-native';
import { Colors } from '../../theme/colors';
export default function RoutineTab() {
  return <View style={{ flex: 1, backgroundColor: Colors.background, alignItems: 'center', justifyContent: 'center' }}><Text style={{ color: Colors.snow }}>Build Your Routine</Text></View>;
}
