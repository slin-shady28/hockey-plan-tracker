import { View, Text } from 'react-native';
import { Colors } from '../../theme/colors';
export default function CoachTab() {
  return <View style={{ flex: 1, backgroundColor: Colors.background, alignItems: 'center', justifyContent: 'center' }}><Text style={{ color: Colors.snow }}>AI Coach</Text></View>;
}
